[Unit]
Description=Auto-pull dotfiles and apply via chezmoi

[Service]
Type=oneshot
ExecStart=/bin/bash -c 'cd "$HOME/.dotfiles" && [[ "$(git rev-parse --abbrev-ref HEAD)" == gitbutler/* ]] && exit 0; chezmoi update --force'
