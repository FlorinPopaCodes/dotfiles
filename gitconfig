[user]
    name = Florin Popa
    email = pp.flrn@gmail.com
    signingkey = pp.flrn@gmail.com
[diff]
    tool = meld
[color]
    ui = auto
    branch = auto
    diff = auto
    interactive = auto
    status = auto
[alias]
    assume   = update-index --assume-unchanged
    unassume = update-index --no-assume-unchanged
    assumed  = "!git ls-files -v | grep ^h | cut -c 3-"
[merge]
    tool = meld
[difftool]
    prompt = false
[mergetool]
    prompt = false
[rerere]
    enabled = 1
[push]
    default = upstream
[core]
	excludesfile = /home/florin/.gitignore_global
